[package]
name = "fixture-feature-gap"
version = "0.0.0"
edition = "2024"
publish = false

[features]
default = []
broken = []

# Keep this deliberately broken fixture outside the parent workspace.
[workspace]
