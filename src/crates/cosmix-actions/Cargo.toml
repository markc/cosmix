[package]
name = "cosmix-actions"
version = "0.4.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Mesh-free action registry and data-driven keyboard resolution for Cosmix apps"

[lib]
name = "cosmix_actions"
path = "src/lib.rs"

[dependencies]
cosmix_config = { package = "cosmix-lib-config", path = "../cosmix-lib-config" }
serde.workspace = true
thiserror = "2"
