{
  "name": "unchanged reload finishes on its reply",
  "about": "edit.reload with nothing new on disk returns {unchanged:true} and publishes no event (editd actor.rs:1207); the Server slot clears on the reply alone.",
  "path": "/fixture/doc.txt",
  "initial": "hello world\n",
  "run_id": 1,
  "steps": [
    {"server_op": {"id": "R1", "op": {"reload": {"force": false}}}},
    {"send": {"id": "R1", "verb": "edit.reload", "caller": "local:ced",
      "args": {"buffer": "b1_0000e1e1", "force": false, "origin": "human:ced", "op_id": "c00000001-000001"},
      "expect": {"rc": 0, "rev": 0, "unchanged": true}}},
    {"deliver_reply": "R1"}
  ],
  "expect": {"server_text": "hello world\n", "server_rev": 0, "converged": true, "conflicts": 0, "detached": false, "idle": true}
}
