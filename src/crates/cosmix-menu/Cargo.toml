[package]
name = "cosmix-menu"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "System tray app launcher menu for Cosmix"

[[bin]]
name = "cosmix-menu"
path = "src/main.rs"

[dependencies]
ksni = { version = "0.3", features = ["blocking"] }
mimalloc.workspace = true
dirs-next = "2"
