[package]
name = "cosmix-interaction-schema"
version = "0.6.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "interact.* wire contract for the ctkd ephemeral-surfaces daemon — headless serde DTOs starting with notify.v1. Pure Rust, no Bevy/cosmix deps; CTK integration arrives with dialog.v1 Phase 2/3."

[lib]
name = "cosmix_interaction_schema"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
