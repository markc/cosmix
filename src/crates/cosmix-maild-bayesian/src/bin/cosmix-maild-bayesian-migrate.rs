//! Phase 0 placeholder for the migration tool. The full tool, which
//! converts a spamlite v0.4.1 database into the cosmix-maild-bayesian
//! v2 schema (adding `model_version` + `seeded_from` columns), lands
//! in Phase 5.

fn main() {
    eprintln!("cosmix-maild-bayesian-migrate is a Phase 0 stub; Phase 5 wires the real migration.");
    std::process::exit(2);
}
