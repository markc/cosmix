[package]
name = "cosmix-interaction-broker"
version = "0.5.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Headless decision core for the ctkd interact.* broker — notify.v1 origin labelling, per-origin/global rate-limiting, dedupe/coalesce, remote urgency-clamp, and dispatch-registration enforcement."

[lib]
name = "cosmix_interaction_broker"

[dependencies]
cosmix-interaction-schema = { path = "../cosmix-interaction-schema" }
subtle = "2.6"

[dev-dependencies]
serde_json = { workspace = true }
