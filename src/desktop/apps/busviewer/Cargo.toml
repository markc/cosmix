[package]
name = "cosmix-busviewer"
version = "0.1.2"
description = "CTK service browser and verb caller for the ABP Bus"
edition = "2021"
rust-version = "1.95"
license = "MIT"

[package.metadata.cosmix]
kind = "app"
component = "busviewer"
display = "BusViewer"
engine = "bevy"

[[bin]]
name = "cosmix-busviewer"
path = "src/main.rs"

[dependencies]
# `--version` build provenance (cosmix_buildinfo::exit_on_version!).
cosmix_buildinfo = { package = "cosmix-lib-buildinfo", path = "../../../crates/cosmix-lib-buildinfo" }
bevy = { version = "=0.19.0", default-features = false, features = ["ui", "bevy_feathers", "accesskit_unix"] }
ctk = { version = "=0.58.1", path = "../../ctk", features = ["bus", "theme", "system-clipboard"] }
cosmix-client = { package = "cosmix-lib-client", path = "../../../crates/cosmix-lib-client" }
flume = "0.11"
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "time", "sync"] }

[build-dependencies]
cosmix_buildinfo = { package = "cosmix-lib-buildinfo", path = "../../../crates/cosmix-lib-buildinfo" }
