# 2026-03-31 — Repo restructure, Lua removal, Mix integration

## Repo moved: ~/.gh/cosmixos -> ~/.cosmix

- Promoted cosmix to a top-level workspace at `~/.cosmix/` (was `~/.gh/cosmixos/`)
- Renamed GitHub repo from `markc/cosmixos` to `markc/cosmix`
- Updated git remote URL to `git@github.com:markc/cosmix.git`
- Fixed all path dependencies broken by the move (spamlite, mix-core, cosmix-lib-amp)

## Directory restructure: src/ inner workspace

Adopted the same layout as `~/.mix/` — clean top-level for GitHub/humans, build infrastructure in `src/`:

**Top level (visible on GitHub):**
- `CLAUDE.md`, `README.md`, `LICENSE`, `.mcp.json`, `.gitignore`

**Moved into `src/`:**
- `Cargo.toml`, `Cargo.lock`, `crates/`, `target/`
- `_doc/`, `_journal/`, `_notes.md`, `_etc/`
- `db/`, `scripts/`

All Cargo.toml path dependencies updated to account for the extra directory level.

## Lua fully removed

- Removed `#[serde(alias = "lua_scripts_dir")]` compat shim from `cosmix-lib-config/src/settings.rs`
- Removed `.lua` from file picker extensions in `cosmix-edit/src/main.rs`
- Updated all documentation references from "Lua + Bash" to "Mix + Bash"
- Updated global CLAUDE.md: "Rust + Lua" -> "Rust + Mix", shell entry updated to Mix
- Mix (`~/.mix/`) is now the sole scripting language — pure Rust, AMP-native, WASM-compatible

## New files

- `LICENSE` — MIT, Copyright 2026 Mark Constable mc@cosmix.dev
- `README.md` — comprehensive project overview (~350 lines): architecture, AMP protocol, all 28 crates, JMAP server, mesh networking, DCS shell, Mix integration, ARexx lineage, design philosophy

## CLAUDE.md updates

- Added `cosmix-lib-script` to libraries table
- Added Mix to key decisions
- Added mix-core as external path dependency
- Updated all path references for src/ layout
- Updated build commands to `cd src` first
- Added workspace layout description

## Cross-repo path fixes

| File | Change |
|------|--------|
| `src/Cargo.toml` | spamlite: `../.gh/spamlite` -> `../../.gh/spamlite` |
| `src/crates/cosmix-lib-script/Cargo.toml` | mix-core: 3 levels -> 4 levels (`.mix/src/crates/mix-core`) |
| `src/crates/cosmix-scripts/Cargo.toml` | mix-core: same fix |
| `~/.mix/src/crates/mix-amp/Cargo.toml` | cosmix-lib-amp: updated to `.cosmix/src/crates/cosmix-lib-amp` |
| `.gitignore` | `/target` -> `src/target`, `Cargo.lock` -> `src/Cargo.lock` |

## Memory system migrated

- Copied 9 memory files from old project dir (`-home-cosmix--gh-cosmixos`) to new (`-home-cosmix--cosmix`)
- Added memories: `project_repo_moved.md`, `reference_mix_shell.md`, `feedback_no_lua.md`
- Updated global `~/.claude/CLAUDE.md` workspace table: added `~/.cosmix/`, renamed `~/.rc/` to `~/.mix/`
