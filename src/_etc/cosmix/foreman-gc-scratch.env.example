# Absolute path to the project manifest the timer should maintain.
FOREMAN_PROJECT=/absolute/path/to/project.mix

# Put scratch_pool in the foreman.conf.mix beside that project's ledger so
# pressure escalation reads the real ZFS pool capacity. The timer also uses
# scratch_terminal_age_hours, scratch_pressure_percent and
# scratch_shared_max_gb from that file.
