[package]
name = "cosmix-markdown"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
libcosmic.workspace = true
pulldown-cmark = { version = "0.12", default-features = false, features = ["html"] }
