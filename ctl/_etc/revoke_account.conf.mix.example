-- Private hub configuration for revoke_account.mix.
-- Copy this file to `_etc/revoke_account.conf.mix` in the control hub and
-- replace the placeholders with rostered nodes that run the mail services.
revoke_account: {
  schema_version: 1,
  default_nodes: ["alpha", "beta", "gamma"]
}
