-- deploy_wg.conf.mix.example — private settings for deploy_wg.mix.
-- Copy to deploy_wg.conf.mix in your private control hub and edit it there.
-- The public starter deliberately does not ship an active configuration.
deploy_wg: {
  schema_version: 1,
  expected_mesh: "example.org"
}
